version: 2
jobs:
  build:
    general:
      branches:
        only:
          - feature/k8s
          - staging
          - master
    machine:
      services:
        - docker
    steps:
      - checkout
      - run: chmod +x build.sh && ./build.sh